module.exports = {
    readCoolFile : function(filename) {
        var labelName = "";
        var labels = {};

        try {
            // TODO: @Dennis implement file reading stuff here (lines 35 - 38)
        } catch (e) {
            console.log(e);
        }
    },
    /**
     * Parse the geneLights input file and store info in the AnchorList objects
     * @param url {URL}
     * @param genes {GeneList}
     */
    readFileFromURL : function(url, genes) {
        
    }
};