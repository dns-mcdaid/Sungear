/**
 * Signal is an enum class which allows visGene to send p5
 * unusal commands beyond just drawings.
 * @type {{LOAD: number, SCREENSHOT: number, FULLSCREEN: number}}
 */
module.exports = {
    LOAD : 0,
    SCREENSHOT : 1,
    FULLSCREEN : 2
};